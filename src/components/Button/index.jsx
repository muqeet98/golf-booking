const index = (props) => {
    return (
        <div>
            <button class="about-view travel-btn">
                {props.btn_title}
            </button>
        </div>
    );
};
export default index;
