import React from 'react';
const index = (props) => {
    return (
        <div>
            <img src={props.image} className="listimagesize" alt="package-place"></img>
        </div>
    );
};
export default index;
